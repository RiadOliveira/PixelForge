import { RGBValuesData } from 'types/RGBValuesData';

export const generateDefaultRGBValuesData = (): RGBValuesData => ({
  RED: {
    min: 0,
    max: 0,
  },
  GREEN: {
    min: 0,
    max: 0,
  },
  BLUE: {
    min: 0,
    max: 0,
  },
});
